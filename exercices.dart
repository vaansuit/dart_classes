/* 
  Create an algorithm that has a Map and inside of that map one of the itens is a 
  tuped List with a few itens inside. 
  After that, print the key value of the map
*/

/* 
  Create a list of Sets, the itens of those Sets are maps of 1 item
*/

void main() {
  // Map myMap = {
  //   'name': 'Invoker',
  //   'stats': ['Strength', 'Agility', 'Inteligence']
  // };

  // // print(myMap[[1]]);

  // Map h = {'hero': 'Faceless'};
  // Set hero = {h};
  // Map s = {'status': 'agility'};
  // Set stats = {s};
  // Map i = {'itens': 'aghanins'};
  // Set items = {i};

  // List myList = [hero, stats, items];

  // print(myList[0]);
}
